.row
  - Activity::ACTIVITY_TYPE.each do |activity_type|
    .col-md-6.light-blue
      = activity_type[0]
      = radio_button_tag 'activity[activity_type]', activity_type[1], activity_type[1].to_i == @activity.activity_type, class: 'pull-right', disabled: defined?(disable) && disable

:javascript
  $( document ).ready(function() {
    $(".online_course_image").hide();
    $("input[type='radio'][name='activity[activity_type]']").click(function(){
      $(".online_course_image").hide();
      $(".mcq").show();
      if($(this).attr("value")=="5"){
        $(".online_course_image").show();
        $(".mcq").hide();
      }
    });
    if($("input[type='radio'][name='activity[activity_type]']:checked").val()=="5"){
      $(".online_course_image").show();
      $(".mcq").hide();
    }
  });
