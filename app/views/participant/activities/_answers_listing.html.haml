.leftblock.width-100-perc.break-long-url.max-width-360
  %p.reading
    %span after doing this activity...
    %br
      .lowercase-font
        = @mcq.question_text
  %ul.answer
    - disabled_attr_value = answer_disabled_attr(@activity)
    = hidden_field_tag 'activity_id', @activity.id
    - @mcq.answers.each do |answer|
      %li{ class: highlighter_class_for_answer(answer, @activity) }
        %label.width-60-perc= answer.answer_text
        = check_box_tag 'activity[answer]', answer.id, nil, disabled: disabled_attr_value, class: answer_check_box_class(answer, @activity), id: "activity_answer_#{answer.id}"
.rightblock &nbsp;